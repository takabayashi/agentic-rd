fastapi==0.136.3
uvicorn[standard]==0.49.0
jinja2==3.1.6
psycopg[binary]==3.3.4
psycopg-pool==3.3.1

# Test-only (kept here so the CI test job and `docker compose run` share one pin set)
httpx==0.28.1
pytest==9.0.3
