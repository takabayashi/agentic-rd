fastapi==0.136.3
uvicorn[standard]==0.49.0
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
pydantic-settings==2.7.1
prometheus-client==0.21.1
alembic==1.14.0

# Test-only (kept here so the CI test job and `docker compose run` share one pin set)
# Starlette 1.2+ TestClient expects httpx2 (not httpx) — avoids StarletteDeprecationWarning.
httpx2==2.3.0
pytest==9.0.3
# Integration tests against a throwaway Postgres; skipped automatically when
# Docker isn't available (see tests/test_integration_db.py).
testcontainers[postgres]==4.9.0
